package com.ferrisys.config.web;

/**
 * CORS is centrally configured in SecurityConfig via CorsConfigurationSource.
 * This class is intentionally not annotated with @Configuration to avoid duplicate mappings.
 */
public final class WebCorsConfig {

    private WebCorsConfig() {
    }
}
